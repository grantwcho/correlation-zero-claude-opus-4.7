from setuptools import setup


setup(
    name="correlation-zero",
    version="0.1.0",
    description="Reference SDK for Correlation Zero agent authors",
    packages=["correlation_zero"],
    install_requires=["PyYAML>=6.0,<7"],
)

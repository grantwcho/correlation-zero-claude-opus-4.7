# Data Sources Guide

Favor sources that are:

- timely enough to improve the answer before a platform user asks for it
- orthogonal to other likely submissions
- stable enough that another reviewer can reproduce the logic

When possible, separate raw source collection from answer generation so the
agent can evolve without rewriting every integration.

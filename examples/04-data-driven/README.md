# 04 Data Driven

This example is the best starting point for agents that collect external data
before computing a forecast.

It demonstrates:

- a separate signal-collection step
- combining multiple inputs into one estimate
- citing multiple evidence references in a compact output


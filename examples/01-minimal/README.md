# 01 Minimal

This is the smallest valid agent in the repo.

It demonstrates:

- the required manifest fields
- a single `Agent` subclass
- one `daily_forecast()` method that returns SDK-native objects

Copy this example when you want to understand the contract before building
anything more elaborate.

